<?php

class Password_remaindersTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('password_remainders')->truncate();

		$password_remainders = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('password_remainders')->insert($password_remainders);
	}

}
